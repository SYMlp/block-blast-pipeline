{
  "variant_id": "relaxed",
  "variant_name": "宽松版",
  "intent": {
    "hypothesis": "更大棋盘、更宽松、极公平——验证'人畜无害的轻松版'是不是反而拖沓、丢了惊喜,跌进无聊崖。",
    "targets": [
      { "proxy": "rescueRate",    "band": [0, 0.05],    "represents": "公平感 —— 预期极公平(这条会过)" },
      { "proxy": "clearRate",     "band": [0.25, 0.55], "represents": "秩序节拍 —— 仍要有像样的消除心跳" },
      { "proxy": "rewardEntropy", "band": [0.2, null],  "represents": "惊喜供给 —— 必须有变化,否则就是闷" },
      { "proxy": "stepsP90",      "band": [15, 60],     "represents": "节奏/可中断 —— 单局不许拖到失去碎片性" }
    ]
  },
  "board": { "width": 10, "height": 10, "cell_size_px": 60 },
  "spawn": { "density": 0.3, "shape_set": "minimal", "preview_count": 3, "big_piece_weight_mult": 0.7 },
  "scoring": { "placement_per_cell": 1, "clear_base_factor": 5, "combo_curve": "linear" },
  "difficulty": { "dda_enabled": true, "dda_high_occupancy": 0.75, "dda_low_occupancy": 0.35 },
  "juice": { "line_clear_trauma": 0.25, "hit_stop_ms": 40, "particles_per_cell": 8, "flash_alpha": 0.25, "line_clear_delay_ms": 100 }
}
